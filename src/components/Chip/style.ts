import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderRadius: 20,
    paddingVertical: 8,
    paddingHorizontal: 20,
    borderColor: "#4D1BFD",
    borderWidth: 1,
  },
  label: {
    fontSize: 14,
    textAlign: "center",
  },
});
